# Проверка исправления стандарта, 05.09.2026

Статус: предварительное превью; браузерные проверки пройдены, общий допуск не пройден.
Профиль: arhitektura.home.v3.1. Основание и границы: DELTA-standards.md.

- standards-check.js: 50/50, 0 ошибок. Ширины 320/360/390/768/1440, масштаб 200%, загруженные шрифты, две поддержки, верхняя <1/3, плавающая кнопка скрывается, переходы в пределах страницы, пять способов помощи, календарь последний, три карточки, 365 дней, ракурсы, школьные режимы и дни календаря.
- browser-check.js: 29/29, 0 ошибок, включая каталог: поиск, пустое состояние, фильтры, пагинация 12/страница, последние 5, сохранение маршрута, возвращение из истории; ошибок JS 0.
- page_gate check: 37/39, две ошибки: общий audit-pages не принимает расположение lab; календарный валидатор игнорирует base URL. Данные календаря разобраны: 365, семь предметных адресов проверены браузерной регрессией и существованием файлов в muzei/arhitektura.
- gate.py: 2/3 (трещотка и навыки пройдены; тот же неизвестный lab-профиль).
- git diff --check и node --check support-adapter.js пройдены.

Точная команда браузера: `npx --yes --package @playwright/cli playwright-cli --session codex-bot run-code --filename <standards-check.js|browser-check.js>`.
Локальный HTML и support-adapter подставлялись через page.route на собственную вкладку по исходному публичному URL; общие скрипты, изображения и шрифты загружались с публичного сайта. Это проверка подготовленного кода в реальном браузере, НЕ доказательство публикации исправлений.
Полные скриншоты 390 и 1440 просмотрены; проверены помощь, текст и нижняя навигация/календарь. Второй пакет исправил перенос длинной ссылки при 200%; ожидание прокрутки проверяет геометрию вместо произвольной паузы.

Матрица замечаний: шапка → раздельный бренд и H1 + слоганы → первые 5 проверок; помощь → общий движок + адаптер → 5 способов, 2 блока, геометрия всех ширин, переходы, скрытие; календарь → после подвала → 5 проверок конца документа; сохранность → 29 браузерных регрессий.
Текст: новые формулировки только служебные — способы помощи и самостоятельная строка существующего слогана; предметные факты, подписи и хук Кижей не менялись. Ролевые проходы выполнены автором, независимая рецензия не заявляется.

Публикация не выполнена: общий шлюз не зелёный. Долги и предложения переданы Jarvis нарядом 2026-09-05-architecture-home-standards.md, очередь 136. Следующий шаг — принять контракт lab/главной и исправить валидаторы, затем пройти единый шлюз и опубликовать этот пакет; прежний адрес пока показывает прежнюю версию.
